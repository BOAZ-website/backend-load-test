global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spring-boot'
    static_configs:
      - targets: ['host.docker.internal:9091']  # SSH 터널 포트 (로컬 9091 → EC2 8080)
    metrics_path: '/actuator/prometheus'
